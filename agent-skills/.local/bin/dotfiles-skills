#!/usr/bin/env sh
set -eu

script_path=$(readlink -f "$0" 2>/dev/null || printf '%s' "$0")
repo_dir=$(CDPATH= cd -- "$(dirname -- "$script_path")/../../.." && pwd)
exec python3 "$repo_dir/scripts/dotfiles-skills.py" "$@"
